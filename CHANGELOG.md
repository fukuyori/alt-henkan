# Changelog

Alt Henkan の変更履歴です。Git履歴に基づき、バージョンごとの主な変更を記載します。

## 0.3.1

### 追加

- EmacsモードにAlt+<／Alt+>（文書の先頭／末尾）、Ctrl+D（Delete）、Alt+D（Ctrl+Delete）、Ctrl+K（行末まで削除）を追加。各操作を個別にON／OFFできます。
- Emacsモード全体で、Ctrl・Altそれぞれの対象を「左」「右」「両方」から選ぶ設定を追加。既定値は「両方」で、左右どちらでも操作できます。Alt単独のIME切り替え設定には影響しません。
- 左右の指定、旧設定の読み込み、追加操作の入力イベントとキー解放処理のテストを追加。

### 変更

- Alt+<／Alt+>ではUS配列のAlt+Shift+`,`／Alt+Shift+`.`を変換対象とし、移動先の選択にならないようShiftを一時解放して復元。
- Ctrl+KはShift+Endで選択してDeleteを送信。行末では改行を削除して次の行を結合します。実際の範囲は対象アプリのEnd・Delete動作に従います。削除操作にクリップボード保存やkill ringは含みません。

## 0.3.0

### 追加

- CapsLockで通常モード／Emacsモードを切り替える機能を追加。機能は初期状態ではOFFで、起動時は通常モードになります。
- カーソル移動を中心とする8種類のショートカットを追加し、それぞれ個別にON／OFFできるようにしました。
  - Ctrl+B／Ctrl+F：左／右
  - Ctrl+P／Ctrl+N：上／下
  - Ctrl+A／Ctrl+E：Home／End（行頭／行末）
  - Alt+B／Alt+F：Ctrl+左／Ctrl+右（前／次の単語）
- モード切り替え時の画面表示を追加。表示のON／OFFを選択でき、現在のモードは通知領域のメニューとツールチップでも確認できます。
- Emacsモードのキー押下・解放、設定の互換性、入力イベントの構造などのテストを追加。

### 変更

- 設定画面を「Alt設定」「Emacs設定」「表示設定」の3タブに統合。
- 既存のAlt設定を引き継ぎながら、Emacs機能全体と各移動操作の設定を保存できるようにしました。
- `build-installer.ps1` で `-Sign` と `-NoUiAccess` のどちらも指定されていない場合は、例外ではなく署名の必要性と実行例を表示して終了するように変更。この場合、ビルド・署名・インストーラー作成は行いません。

Emacsモードでも通常の文字入力は変更しません。ONにした移動操作だけを変換し、追加の修飾キーを含む組み合わせは変換しません。Alt+B／Alt+Fを移動として使用した後は、Alt解放時のIME切り替えを送りません。viモードや文字編集の操作は含みません。

## 0.2.0

### 改善

- 入力フックを設定画面とは別の専用スレッドへ移し、画面処理による入力処理の停滞を避ける構成に変更。
- 長時間稼働時のフック維持のため、30秒ごとに再登録を予約し、1秒以上無入力かつキー・マウスボタンが押されておらず、Alt操作の途中でもない時点で再登録する処理を追加。
- スリープ復帰・ロック解除・アプリの「有効」のオフ→オン時にもフック再登録を予約するように変更。
- 診断ログの書き込みを容量制限付き非同期キューへ移し、ディスク待ちが入力処理を妨げない構成に変更。入力送信失敗の通知表示もフック処理から分離。
- フックの再登録世代・コールバック回数・遅いコールバックの診断情報と、フック登録・自動復旧などのテストを追加。

### 修正

- キー解放を取り逃した状態が、その後のAlt単独押しの判定に残り続けないよう、押下状態を照合する処理を追加。

## 0.1.0

### 追加

- Windowsの通知領域に常駐するAlt Henkanを作成。
- 左Alt単独押しでIMEオフ、右Alt単独押しでIMEオンを送信。物理的な変換・無変換キーのないUSキーボードでも利用できる構成にしました。
- Alt単独長押し後の解放で、Windows本来のAlt単独操作を実行。左右個別の有効／無効設定と長押し判定時間の設定を追加。
- 他のキーやマウス操作と組み合わせた場合は、通常のAlt操作を通す処理を追加。
- トレイからの有効／無効切り替え、設定保存、診断ログを追加。
- リリースビルドとInno Setupインストーラーの作成スクリプト、`CODESIGN_CERT` による実行ファイル・インストーラー・アンインストーラーの署名処理を追加。
- 管理者権限のウィンドウに対応する署名付きuiAccess版と、`-NoUiAccess` による通常権限のユーザー単位インストーラーに対応。
